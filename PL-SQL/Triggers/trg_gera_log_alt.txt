CREATE TABLE LOG
( USUARIO VARCHAR2(30),
  DATA DATE
);

CREATE OR REPLACE TRIGGER gera_log_alt
AFTER UPDATE OF vl_produto ON Produto
DECLARE
BEGIN
  INSERT INTO LOG(Usuario, DATA) VALUES (USER, SYSDATE);
END;
